export type TargetMonth = {
  id: string;
  month: string;
  target: string;
  achievement: boolean;
  belongQuoter: string;
};
export type TargetQuoter = {
  id: string;
  quoter: string;
  target: string;
  grade: number;
};
