export type TargetMonth = {
  id: string;
  month: string;
  target: string;
  achievement: boolean;
  belong: string;
};
export type TargetQuoter = {
  id: string;
  quoter: number;
  target: string;
};
